@using Cocktails07
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
       <meta name="viewport" content="width=device-width" />
        <meta name="description" content="Graphic turorial to remember or learn how to make basic cocktails." />

@*        The last line is guilty of modifying the screen when it is a mobil device*@
                    <title>@ViewBag.Title</title>
        @Styles.Render("~/bundles/MobileCSS")
        @Scripts.Render("~/bundles/modernizr")
        @Scripts.Render("~/bundles/MobileJS")
    </head>
    <body>
                        @*@{
                            bool ShowBackButtom = (bool)(ViewBag.ShowBackButton ?? true);
                            bool ShowHomeButtom = (bool)(ViewBag.ShowHomeButton ?? true);
                            }*@
        <div data-role ="page" data-theme="b">
        <div data-role="header" data-theme="b" data-position="fixed">
            @*                    <h1>@ViewBag.Title</h1>
        @if (ShowBackButtom)
        {
            <a data-rel="back" data-role="button" data-transition="slide" data-direction="reverse" data-icon="back">Back</a>
        }
        @if (ShowHomeButtom)
        {
            <a href="@Url.Action("Index","Home")" data-role="button" data-trsnsition="slide" data-direction="reverse" data-icon="home">Home</a>
        }*@
                @*@Html.Partial("_LoginPartialPhone")*@
                <div data-role="navbar" data-theme="b">
                    <ul id="menu">
                        <li>@Html.ActionLink("Home", "Index", "Home")</li>
                        <li>@Html.ActionLink("About", "About", "Home")</li>
                        <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                        @*<li>@Html.ActionLink("Support", "Support", "Home")</li>*@
                    </ul>
                </div>
            </div>
                    <div data-role="content" data-theme="b">
            @RenderSection("featured", required: false)
            @RenderBody()
        </div>
        @*<div data-role="footer" data-theme="b">*@
            <p>&copy; @DateTime.Now.Year</p>
        @*</div>*@
        @RenderSection("scripts", required: false)
    </div>
    </body>
</html>
